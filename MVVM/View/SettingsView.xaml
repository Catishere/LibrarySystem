<UserControl x:Class="LibrarySystem.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibrarySystem.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="83*"/>
            <RowDefinition Height="110*"/>
            <RowDefinition Height="257*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="129*"/>
            <ColumnDefinition Width="171*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Настройки" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="54" Width="180" FontSize="22"/>
        <TextBox Style="{StaticResource TextBoxStylish}" HorizontalAlignment="Center" Height="31" Margin="0" TextWrapping="Wrap" Text="{Binding SuggestionCount}" VerticalAlignment="Center" Width="168" Grid.Column="1" Grid.Row="1"/>
        <Label Content="Брой подсказки:" HorizontalAlignment="Right" Margin="0" VerticalAlignment="Center" Width="118" Grid.Row="1" Height="26"/>
        <Button Command="{Binding UpdateSettingsCommand}" Style="{StaticResource ButtonStyle}" Content="Запази" HorizontalAlignment="Right" Margin="0" VerticalAlignment="Top" Grid.Row="2" Height="26" Width="125"/>
        <Border 
            Background="#FFBDFFB7" BorderThickness="1" Grid.Column="2" HorizontalAlignment="Stretch" Height="63" Margin="10" VerticalAlignment="Stretch">
            <TextBlock 
                Text="{Binding StatusMessage}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasStatus}" Value="True">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

    </Grid>
</UserControl>
